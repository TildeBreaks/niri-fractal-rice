# Kitty Terminal Configuration
# Beautiful, fast, and feature-rich

# Font Configuration
font_family      JetBrainsMono Nerd Font
bold_font        JetBrainsMono Nerd Font Bold
italic_font      JetBrainsMono Nerd Font Italic
bold_italic_font JetBrainsMono Nerd Font Bold Italic
font_size 11.0

# Adjust line height for better readability
adjust_line_height  110%
adjust_column_width 0

# Cursor
cursor_shape beam
cursor_beam_thickness 1.5
cursor_blink_interval 0.5
cursor_stop_blinking_after 15.0

# Scrollback
scrollback_lines 10000
scrollback_pager less --chop-long-lines --RAW-CONTROL-CHARS +INPUT_LINE_NUMBER
wheel_scroll_multiplier 5.0

# Mouse
mouse_hide_wait 3.0
url_color #0087bd
url_style curly
open_url_with default
copy_on_select yes
strip_trailing_spaces smart

# Performance
repaint_delay 10
input_delay 3
sync_to_monitor yes

# Bell
enable_audio_bell no
visual_bell_duration 0.0
window_alert_on_bell yes
bell_on_tab yes

# Window Layout
remember_window_size  yes
initial_window_width  1000
initial_window_height 600
window_padding_width 8
window_margin_width 0
placement_strategy center
hide_window_decorations yes

# Tab Bar
tab_bar_edge top
tab_bar_margin_width 0.0
tab_bar_style powerline
tab_powerline_style slanted
tab_title_template "{index}: {title}"
active_tab_font_style   bold
inactive_tab_font_style normal

# Color scheme will be set by pywal
include ~/.cache/wal/colors-kitty.conf

# Advanced
shell .
editor .
close_on_child_death no
allow_remote_control yes
update_check_interval 0
startup_session none
clipboard_control write-clipboard write-primary

# OS Specific
linux_display_server auto

# Keyboard shortcuts
kitty_mod ctrl+shift

# Clipboard
map kitty_mod+c copy_to_clipboard
map kitty_mod+v paste_from_clipboard
map kitty_mod+s paste_from_selection

# Scrolling
map kitty_mod+up        scroll_line_up
map kitty_mod+down      scroll_line_down
map kitty_mod+page_up   scroll_page_up
map kitty_mod+page_down scroll_page_down
map kitty_mod+home      scroll_home
map kitty_mod+end       scroll_end

# Window management
map kitty_mod+enter new_window
map kitty_mod+w close_window
map kitty_mod+] next_window
map kitty_mod+[ previous_window
map kitty_mod+f move_window_forward
map kitty_mod+b move_window_backward

# Tab management
map kitty_mod+t new_tab
map kitty_mod+q close_tab
map kitty_mod+right next_tab
map kitty_mod+left previous_tab
map kitty_mod+. move_tab_forward
map kitty_mod+, move_tab_backward

# Layout management
map kitty_mod+l next_layout
map kitty_mod+z toggle_layout stack

# Font sizes
map kitty_mod+equal     change_font_size all +2.0
map kitty_mod+minus     change_font_size all -2.0
map kitty_mod+backspace change_font_size all 0

# Miscellaneous
map kitty_mod+f11 toggle_fullscreen
map kitty_mod+u kitten unicode_input
map kitty_mod+f2 edit_config_file
map kitty_mod+escape kitty_shell window

# Background opacity
background_opacity 0.95
dynamic_background_opacity yes
map kitty_mod+a>m set_background_opacity +0.1
map kitty_mod+a>l set_background_opacity -0.1
map kitty_mod+a>1 set_background_opacity 1
map kitty_mod+a>d set_background_opacity default

# Blur (if compositor supports it)
background_blur 32

# Fractal Background Image
# This displays a fractal flame as the background
#background_image ~/.config/kitty/fractals/startup_fractal.png
#background_image_layout scaled
#background_tint 0.85

# Regenerate fractal on kitty reload
# map kitty_mod+f5 launch --type=background ~/.config/kitty/generate_fractal.sh
